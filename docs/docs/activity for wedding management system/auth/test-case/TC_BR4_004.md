# Test Case TC_BR4_004

## Verify current user session is cleared via clearCurrentUser()

| Test Case ID | Test Case Description                                         | PreRequisites                                                                                         | Steps | Steps to Perform                                                                                                                                                                            | Step Expected Result                                                                                                                                                                                                     | Test Case Expected Result                                                     | Actual Result | Status | Note |
| ------------ | ------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------- | ------------- | ------ | ---- |
| TC_BR4_004   | Verify current user session is cleared via clearCurrentUser() | 1. WMS application is installed<br>2. User is logged in<br>3. User session contains current user data | 4     | 1. Login with valid credentials (e.g., "Neith")<br>2. Verify user session exists (check Account shows user info)<br>3. Click LogoutButton<br>4. After logout, verify no user session exists | 1. User logged in, session created<br>2. Account view shows current user's info (username, fullname)<br>3. Logout process initiated, clearCurrentUser() called<br>4. User session is null/empty, no user data accessible | User session is completely cleared after logout via clearCurrentUser() method |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                 |
| ------------------ | --------------------- |
| **Test Case ID**   | TC_BR4_004            |
| **Requirement ID** | BR4                   |
| **Use Case**       | UC 2.1.1.2 - Logout   |
| **Test Type**      | Functional / Security |
| **Priority**       | High                  |
| **Automation**     | Manual                |

### Detailed Steps

| Step | Steps to Perform                                           | Step Expected Result                                        |
| ---- | ---------------------------------------------------------- | ----------------------------------------------------------- |
| 1    | Login with valid credentials (e.g., "Neith")               | User logged in, session created                             |
| 2    | Verify user session exists (check Account shows user info) | Account view shows current user's info (username, fullname) |
| 3    | Click LogoutButton                                         | Logout process initiated, clearCurrentUser() called         |
| 4    | After logout, verify no user session exists                | User session is null/empty, no user data accessible         |

### Pass/Fail Criteria

| Criteria        | Pass                                | Fail                     |
| --------------- | ----------------------------------- | ------------------------ |
| Session cleared | ✓ clearCurrentUser() executed       | ✗ Method not called      |
| No user data    | ✓ User session is null/empty        | ✗ User data still exists |
| Security        | ✓ Previous user info not accessible | ✗ Data leakage risk      |
