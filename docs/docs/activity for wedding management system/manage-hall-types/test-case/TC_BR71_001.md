# Test Case TC_BR71_001

## Verify ExportToExcelCommand checks if HallTypeList has data

| Test Case ID | Test Case Description                                       | PreRequisites                                                                                                        | Steps | Steps to Perform                                                                                           | Step Expected Result                                                                                           | Test Case Expected Result                                                   | Actual Result | Status | Note |
| ------------ | ----------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ------------- | ------ | ---- |
| TC_BR71_001  | Verify ExportToExcelCommand checks if HallTypeList has data | 1. WMS application is installed<br>2. User is logged in (Staff/Admin)<br>3. HallTypeView is displayed in Export mode | 3     | 1. Open HallTypeView in export mode<br>2. Click ExportToExcelCommand button<br>3. Observe validation check | 1. Export mode active<br>2. Button clicked<br>3. System checks HallTypeList != null AND HallTypeList.Count > 0 | ExportToExcelCommand validates that HallTypeList has data before proceeding |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                                    |
| ------------------ | ---------------------------------------- |
| **Test Case ID**   | TC_BR71_001                              |
| **Requirement ID** | BR71                                     |
| **Use Case**       | UC 2.1.3.10 - Export Hall Types to Excel |
| **Test Type**      | Functional / Validation                  |
| **Priority**       | High                                     |
| **Automation**     | Manual                                   |

### Expected Validation Logic

```csharp
if (HallTypeList == null || HallTypeList.Count == 0)
{
    // Show MSG19 error
    return;
}
// Proceed with export
```

### Detailed Steps

| Step | Steps to Perform                  | Step Expected Result                                          |
| ---- | --------------------------------- | ------------------------------------------------------------- |
| 1    | Open HallTypeView in export mode  | Export mode active                                            |
| 2    | Click ExportToExcelCommand button | Button clicked                                                |
| 3    | Observe validation check          | System checks HallTypeList != null AND HallTypeList.Count > 0 |

### Pass/Fail Criteria

| Criteria         | Pass                      | Fail                         |
| ---------------- | ------------------------- | ---------------------------- |
| Null check       | ✓ Checks for null         | ✗ No null check              |
| Count check      | ✓ Checks count > 0        | ✗ No count check             |
| Validation first | ✓ Validates before export | ✗ Exports without validation |
