# Test Case TC_BR41_003

## Verify HallViewModel loads halls from database using GetAll() method

| Test Case ID | Test Case Description                                                | PreRequisites                                                                           | Steps | Steps to Perform                                                                                                                          | Step Expected Result                                                                                              | Test Case Expected Result                                                                      | Actual Result | Status | Note |
| ------------ | -------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------------- | ------ | ---- |
| TC_BR41_003  | Verify HallViewModel loads halls from database using GetAll() method | 1. WMS application is installed<br>2. User is logged in<br>3. Hall table has 30 records | 4     | 1. Open HallView<br>2. Check HallList count in ViewModel<br>3. Verify GetAll() method was called<br>4. Compare with database record count | 1. HallView displayed<br>2. HallList populated<br>3. GetAll() invoked<br>4. Count matches Hall table records (30) | HallViewModel correctly loads all hall records from database using HallService.GetAll() method |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                          |
| ------------------ | ------------------------------ |
| **Test Case ID**   | TC_BR41_003                    |
| **Requirement ID** | BR41                           |
| **Use Case**       | UC 2.1.3.1 - View Hall Details |
| **Test Type**      | Functional / Data Loading      |
| **Priority**       | Critical                       |
| **Automation**     | Manual                         |

### Data Loading Logic

```csharp
private void LoadHalls()
{
    var halls = _hallService.GetAll();
    HallList = new ObservableCollection<Hall>(halls);
    OriginalList = halls.ToList();
}
```

### Database Verification

```sql
SELECT COUNT(*) FROM Hall
-- Expected: 30 records (from seed data)
```

### Detailed Steps

| Step | Steps to Perform                   | Step Expected Result                  |
| ---- | ---------------------------------- | ------------------------------------- |
| 1    | Open HallView                      | HallView displayed                    |
| 2    | Check HallList count in ViewModel  | HallList populated                    |
| 3    | Verify GetAll() method was called  | GetAll() invoked                      |
| 4    | Compare with database record count | Count matches Hall table records (30) |

### Pass/Fail Criteria

| Criteria      | Pass               | Fail              |
| ------------- | ------------------ | ----------------- |
| Method called | ✓ GetAll() invoked | ✗ Not called      |
| Count correct | ✓ Matches DB count | ✗ Mismatch        |
| Data complete | ✓ All halls loaded | ✗ Missing records |
