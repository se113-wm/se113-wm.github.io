# Test Case TC_BR51_003

## Verify CanDelete() returns true when hall has no references

| Test Case ID | Test Case Description                                       | PreRequisites                                                                                       | Steps | Steps to Perform                                                                                                             | Step Expected Result                                                                                | Test Case Expected Result                                        | Actual Result | Status | Note |
| ------------ | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | ------------- | ------ | ---- |
| TC_BR51_003  | Verify CanDelete() returns true when hall has no references | 1. WMS application is installed<br>2. User is logged in<br>3. Hall "NewHall" has 0 wedding bookings | 4     | 1. Add a new hall "TestHall" (no weddings)<br>2. Click Delete button<br>3. Select "TestHall"<br>4. Verify CanDelete() result | 1. TestHall created<br>2. Delete mode active<br>3. TestHall selected<br>4. CanDelete() returns true | CanDelete() returns true when hall has no foreign key references |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                    |
| ------------------ | ------------------------ |
| **Test Case ID**   | TC_BR51_003              |
| **Requirement ID** | BR51                     |
| **Use Case**       | UC 2.1.3.4 - Delete Hall |
| **Test Type**      | Validation / Positive    |
| **Priority**       | High                     |
| **Automation**     | Manual                   |

### Reference Check

```sql
SELECT COUNT(*) FROM Wedding WHERE HallId = @TestHallId
-- Should return: 0
```

### Expected Flow

```csharp
// TestHall has no weddings
if (_hallService.CanDelete(testHallId))  // Returns true
{
    // Proceed to confirmation dialog
}
```

### Detailed Steps

| Step | Steps to Perform                        | Step Expected Result |
| ---- | --------------------------------------- | -------------------- |
| 1    | Add a new hall "TestHall" (no weddings) | TestHall created     |
| 2    | Click Delete button                     | Delete mode active   |
| 3    | Select "TestHall"                       | TestHall selected    |
| 4    | Verify CanDelete() result               | CanDelete() = true   |

### Pass/Fail Criteria

| Criteria     | Pass                 | Fail              |
| ------------ | -------------------- | ----------------- |
| Check passes | ✓ Returns true       | ✗ Returns false   |
| No MSG40     | ✓ No error shown     | ✗ Error displayed |
| Proceeds     | ✓ Shows confirmation | ✗ Blocked         |
