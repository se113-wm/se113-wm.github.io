# Test Case TC_BR87_001

## Verify system checks if DishList has data before export

| Test Case ID | Test Case Description                                   | PreRequisites                                                                                                    | Steps | Steps to Perform                                                                                     | Step Expected Result                                                                                       | Test Case Expected Result                        | Actual Result | Status | Note |
| ------------ | ------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | ------------- | ------ | ---- |
| TC_BR87_001  | Verify system checks if DishList has data before export | 1. WMS application is installed<br>2. User is logged in (Staff/Admin)<br>3. FoodView is displayed in Export mode | 3     | 1. Select "Xuất Excel" action<br>2. Click ExportToExcelCommand button<br>3. Observe validation check | 1. Export mode activated<br>2. Export initiated<br>3. System checks DishList != null && DishList.Count > 0 | System validates DishList has data before export |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                                |
| ------------------ | ------------------------------------ |
| **Test Case ID**   | TC_BR87_001                          |
| **Requirement ID** | BR87                                 |
| **Use Case**       | UC 2.1.3.15 - Export Dishes to Excel |
| **Test Type**      | Functional / Validation              |
| **Priority**       | High                                 |
| **Automation**     | Manual                               |

### Expected Validation

```csharp
if (DishList == null || DishList.Count == 0)
{
    // Show MSG19
    return;
}
```

### Detailed Steps

| Step | Steps to Perform                  | Step Expected Result                                 |
| ---- | --------------------------------- | ---------------------------------------------------- |
| 1    | Select "Xuất Excel" action        | Export mode activated                                |
| 2    | Click ExportToExcelCommand button | Export initiated                                     |
| 3    | Observe validation check          | System checks DishList != null && DishList.Count > 0 |

### Pass/Fail Criteria

| Criteria       | Pass              | Fail             |
| -------------- | ----------------- | ---------------- |
| Check null     | ✓ Validates null  | ✗ No null check  |
| Check empty    | ✓ Validates count | ✗ No count check |
| Before process | ✓ Checked first   | ✗ After export   |
