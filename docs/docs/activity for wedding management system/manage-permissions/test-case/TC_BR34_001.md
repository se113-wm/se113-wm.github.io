# Test Case TC_BR34_001

## Verify hasReferences() checks if users exist in selected group

| Test Case ID | Test Case Description                                          | PreRequisites                                                                                                                                                      | Steps | Steps to Perform                                                                                                                                     | Step Expected Result                                                                                                         | Test Case Expected Result                                                                   | Actual Result | Status | Note |
| ------------ | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------- | ------ | ---- |
| TC_BR34_001  | Verify hasReferences() checks if users exist in selected group | 1. WMS application is installed<br>2. Admin user is logged in<br>3. PermissionView is open<br>4. "Delete" action selected<br>5. "Staff" group has 3 users assigned | 4     | 1. Open PermissionView and select "Delete" action<br>2. Select "Staff" group with users<br>3. Click Delete button<br>4. Verify hasReferences() check | 1. Delete mode active<br>2. "Staff" selected<br>3. Delete initiated<br>4. hasReferences() returns TRUE, users found in group | hasReferences() method queries AppUser table to check if any users belong to selected group |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                                |
| ------------------ | ------------------------------------ |
| **Test Case ID**   | TC_BR34_001                          |
| **Requirement ID** | BR34                                 |
| **Use Case**       | UC 2.1.2.8 - Delete Permission Group |
| **Test Type**      | Functional / Validation              |
| **Priority**       | Critical                             |
| **Automation**     | Manual                               |

### Reference Check Logic

```csharp
private bool HasReferences(string groupId)
{
    return db.AppUsers.Any(u => u.GroupId == groupId);
}
```

### Database Query

```sql
SELECT COUNT(*) FROM AppUser WHERE GroupId = 'STAFF'
-- If count > 0, hasReferences() returns TRUE
```

### Detailed Steps

| Step | Steps to Perform                               | Step Expected Result         |
| ---- | ---------------------------------------------- | ---------------------------- |
| 1    | Open PermissionView and select "Delete" action | Delete mode active           |
| 2    | Select "Staff" group with users                | "Staff" selected             |
| 3    | Click Delete button                            | Delete initiated             |
| 4    | Verify hasReferences() check                   | hasReferences() returns TRUE |

### Pass/Fail Criteria

| Criteria       | Pass                     | Fail           |
| -------------- | ------------------------ | -------------- |
| Check executed | ✓ hasReferences() called | ✗ No check     |
| Correct result | ✓ TRUE when users exist  | ✗ Wrong result |
| Query correct  | ✓ Checks AppUser table   | ✗ Wrong table  |
