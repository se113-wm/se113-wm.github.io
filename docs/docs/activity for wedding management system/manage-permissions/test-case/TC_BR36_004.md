# Test Case TC_BR36_004

## Verify Reset() clears SelectedItem after deletion

| Test Case ID | Test Case Description                             | PreRequisites                                                                                                                                          | Steps | Steps to Perform                                                                                                                                                             | Step Expected Result                                                                                                                    | Test Case Expected Result                                                   | Actual Result | Status | Note |
| ------------ | ------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ------------- | ------ | ---- |
| TC_BR36_004  | Verify Reset() clears SelectedItem after deletion | 1. WMS application is installed<br>2. Admin user is logged in<br>3. PermissionView is open<br>4. "Delete" action selected<br>5. Deletable group exists | 5     | 1. Open PermissionView and select "Delete" action<br>2. Select deletable group<br>3. Click Delete and confirm "Yes"<br>4. Check SelectedItem value<br>5. Observe form fields | 1. Delete mode active<br>2. Group selected, SelectedItem set<br>3. Delete completed<br>4. SelectedItem = NULL<br>5. Form fields cleared | Reset() is called after deletion to clear SelectedItem and reset form state |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                                |
| ------------------ | ------------------------------------ |
| **Test Case ID**   | TC_BR36_004                          |
| **Requirement ID** | BR36                                 |
| **Use Case**       | UC 2.1.2.8 - Delete Permission Group |
| **Test Type**      | Functional / State Management        |
| **Priority**       | Medium                               |
| **Automation**     | Manual                               |

### Reset After Delete

```csharp
private void DeleteConfirmed()
{
    // ... delete operations ...
    db.SaveChanges();

    // Refresh and reset
    RefreshGroupList();
    Reset();  // Clears SelectedItem and form
    ShowMessage(MSG26);  // Success notification
}
```

### Detailed Steps

| Step | Steps to Perform                               | Step Expected Result             |
| ---- | ---------------------------------------------- | -------------------------------- |
| 1    | Open PermissionView and select "Delete" action | Delete mode active               |
| 2    | Select deletable group                         | Group selected, SelectedItem set |
| 3    | Click Delete and confirm "Yes"                 | Delete completed                 |
| 4    | Check SelectedItem value                       | SelectedItem = NULL              |
| 5    | Observe form fields                            | Form fields cleared              |

### Pass/Fail Criteria

| Criteria          | Pass             | Fail              |
| ----------------- | ---------------- | ----------------- |
| SelectedItem NULL | ✓ Cleared        | ✗ Stale reference |
| Form cleared      | ✓ Fields empty   | ✗ Old data shown  |
| Ready for next    | ✓ Can select new | ✗ Blocked state   |
