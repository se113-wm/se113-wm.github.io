# Test Case TC_BR110_005

## Verify MSG75 displayed when ShiftName already exists

| Test Case ID | Test Case Description                                | PreRequisites                                                                                              | Steps | Steps to Perform                                                                                                                     | Step Expected Result                                                                                          | Test Case Expected Result                         | Actual Result | Status | Note |
| ------------ | ---------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ------------- | ------ | ---- |
| TC_BR110_005 | Verify MSG75 displayed when ShiftName already exists | 1. WMS application is installed<br>2. User is logged in (Staff/Admin)<br>3. Shift "Ca Sáng" already exists | 4     | 1. Navigate to ShiftView<br>2. Select "Thêm" from SelectedAction<br>3. Enter ShiftName "Ca Sáng", valid times<br>4. Click Add button | 1. ShiftView displayed<br>2. Add mode activated<br>3. Duplicate name entered<br>4. MSG75: "Tên ca đã tồn tại" | System displays MSG75 when ShiftName is duplicate |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                       |
| ------------------ | --------------------------- |
| **Test Case ID**   | TC_BR110_005                |
| **Requirement ID** | BR110                       |
| **Use Case**       | UC 2.1.3.22 - Add New Shift |
| **Test Type**      | Validation / Negative       |
| **Priority**       | High                        |
| **Automation**     | Manual                      |

### Message Code

| Code  | Vietnamese        | English                   |
| ----- | ----------------- | ------------------------- |
| MSG75 | Tên ca đã tồn tại | Shift name already exists |

### Validation Rule

```csharp
if (ShiftDAO.Exists(ShiftName.Trim()))
{
    ShowMessage("MSG75", "Tên ca đã tồn tại");
    return;
}
```

### Database Constraint

```sql
-- ShiftName has UNIQUE constraint
ALTER TABLE Shift ADD CONSTRAINT UQ_ShiftName UNIQUE (ShiftName);
```

### Detailed Steps

| Step | Steps to Perform                       | Step Expected Result       |
| ---- | -------------------------------------- | -------------------------- |
| 1    | Navigate to ShiftView                  | ShiftView displayed        |
| 2    | Select "Thêm" from SelectedAction      | Add mode activated         |
| 3    | Enter ShiftName "Ca Sáng", valid times | Duplicate name entered     |
| 4    | Click Add button                       | MSG75: "Tên ca đã tồn tại" |

### Pass/Fail Criteria

| Criteria          | Pass              | Fail               |
| ----------------- | ----------------- | ------------------ |
| Duplicate check   | ✓ Check performed | ✗ No check         |
| MSG75 shown       | ✓ Correct message | ✗ Wrong/no message |
| Operation blocked | ✓ Not saved       | ✗ Database error   |
