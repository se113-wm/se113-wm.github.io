# Test Case TC_BR112_002

## Verify form populated with selected shift data

| Test Case ID | Test Case Description                          | PreRequisites                                                                                                    | Steps | Steps to Perform                                                                                                              | Step Expected Result                                                                                                                      | Test Case Expected Result                  | Actual Result | Status | Note |
| ------------ | ---------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ----- | ----------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | ------------- | ------ | ---- |
| TC_BR112_002 | Verify form populated with selected shift data | 1. WMS application is installed<br>2. User is logged in (Staff/Admin)<br>3. Shift "Ca Sáng" exists (08:00-12:00) | 4     | 1. Navigate to ShiftView<br>2. Select "Sửa" from SelectedAction<br>3. Click on "Ca Sáng" in DataGrid<br>4. Verify form fields | 1. ShiftView displayed<br>2. Edit mode activated<br>3. Shift selected<br>4. ShiftName = "Ca Sáng", StartTime = "08:00", EndTime = "12:00" | Form is populated with selected shift data |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                    |
| ------------------ | ------------------------ |
| **Test Case ID**   | TC_BR112_002             |
| **Requirement ID** | BR112                    |
| **Use Case**       | UC 2.1.3.23 - Edit Shift |
| **Test Type**      | Functional / UI          |
| **Priority**       | High                     |
| **Automation**     | Manual                   |

### Data Binding

```xaml
<TextBox Text="{Binding SelectedItem.ShiftName, Mode=TwoWay}" />
<TimePicker SelectedTime="{Binding SelectedItem.StartTime, Mode=TwoWay}" />
<TimePicker SelectedTime="{Binding SelectedItem.EndTime, Mode=TwoWay}" />
```

### Detailed Steps

| Step | Steps to Perform                 | Step Expected Result                                          |
| ---- | -------------------------------- | ------------------------------------------------------------- |
| 1    | Navigate to ShiftView            | ShiftView displayed                                           |
| 2    | Select "Sửa" from SelectedAction | Edit mode activated                                           |
| 3    | Click on "Ca Sáng" in DataGrid   | Shift selected                                                |
| 4    | Verify form fields               | ShiftName = "Ca Sáng", StartTime = "08:00", EndTime = "12:00" |

### Pass/Fail Criteria

| Criteria            | Pass           | Fail          |
| ------------------- | -------------- | ------------- |
| Name populated      | ✓ Correct name | ✗ Empty/wrong |
| StartTime populated | ✓ 08:00        | ✗ Empty/wrong |
| EndTime populated   | ✓ 12:00        | ✗ Empty/wrong |
