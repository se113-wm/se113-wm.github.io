# Test Case TC_BR114_002

## Verify Reset() called when entering delete mode

| Test Case ID | Test Case Description                           | PreRequisites                                                                                                       | Steps | Steps to Perform                                                                                                                 | Step Expected Result                                                                                                                     | Test Case Expected Result                     | Actual Result | Status | Note |
| ------------ | ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------- | ------------- | ------ | ---- |
| TC_BR114_002 | Verify Reset() called when entering delete mode | 1. WMS application is installed<br>2. User is logged in (Staff/Admin)<br>3. ShiftView displayed with data in fields | 4     | 1. Navigate to ShiftView<br>2. Select a shift (fields populated)<br>3. Select "Xóa" from SelectedAction<br>4. Verify form fields | 1. ShiftView displayed<br>2. Fields show selected shift data<br>3. Delete mode activated<br>4. Input fields cleared, SelectedItem = null | Reset() clears form when entering delete mode |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                      |
| ------------------ | -------------------------- |
| **Test Case ID**   | TC_BR114_002               |
| **Requirement ID** | BR114                      |
| **Use Case**       | UC 2.1.3.24 - Delete Shift |
| **Test Type**      | Functional / State         |
| **Priority**       | Medium                     |
| **Automation**     | Manual                     |

### Reset Method

```csharp
private void OnActionChanged()
{
    if (SelectedAction == "Xóa")
    {
        IsDeleting = true;
        Reset();
    }
}
```

### Detailed Steps

| Step | Steps to Perform                  | Step Expected Result                      |
| ---- | --------------------------------- | ----------------------------------------- |
| 1    | Navigate to ShiftView             | ShiftView displayed                       |
| 2    | Select a shift (fields populated) | Fields show selected shift data           |
| 3    | Select "Xóa" from SelectedAction  | Delete mode activated                     |
| 4    | Verify form fields                | Input fields cleared, SelectedItem = null |

### Pass/Fail Criteria

| Criteria            | Pass          | Fail         |
| ------------------- | ------------- | ------------ |
| Fields cleared      | ✓ Empty       | ✗ Has values |
| SelectedItem null   | ✓ Reset       | ✗ Still set  |
| Ready for selection | ✓ Clean state | ✗ Stale data |
