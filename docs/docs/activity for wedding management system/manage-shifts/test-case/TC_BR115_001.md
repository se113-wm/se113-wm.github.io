# Test Case TC_BR115_001

## Verify CanDelete() checks Booking table for references

| Test Case ID | Test Case Description                                  | PreRequisites                                                                                      | Steps | Steps to Perform                                                                                                 | Step Expected Result                                                                                                              | Test Case Expected Result                                  | Actual Result | Status | Note |
| ------------ | ------------------------------------------------------ | -------------------------------------------------------------------------------------------------- | ----- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ------------- | ------ | ---- |
| TC_BR115_001 | Verify CanDelete() checks Booking table for references | 1. WMS application is installed<br>2. User is logged in (Staff/Admin)<br>3. Shift "Ca Sáng" exists | 4     | 1. Navigate to ShiftView<br>2. Select "Xóa" from SelectedAction<br>3. Select "Ca Sáng"<br>4. Click Delete button | 1. ShiftView displayed<br>2. Delete mode activated<br>3. Shift selected<br>4. CanDelete() queries Booking table for FK references | CanDelete() checks if shift is referenced in Booking table |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                      |
| ------------------ | -------------------------- |
| **Test Case ID**   | TC_BR115_001               |
| **Requirement ID** | BR115                      |
| **Use Case**       | UC 2.1.3.24 - Delete Shift |
| **Test Type**      | Functional / Database      |
| **Priority**       | High                       |
| **Automation**     | Manual                     |

### CanDelete Method

```csharp
private bool CanDelete(int shiftId)
{
    return !_context.Bookings.Any(b => b.ShiftId == shiftId);
}
```

### Database Constraint

```sql
ALTER TABLE Booking
ADD CONSTRAINT FK_Booking_Shift
FOREIGN KEY (ShiftId) REFERENCES Shift(ShiftId);
```

### Detailed Steps

| Step | Steps to Perform                 | Step Expected Result                                |
| ---- | -------------------------------- | --------------------------------------------------- |
| 1    | Navigate to ShiftView            | ShiftView displayed                                 |
| 2    | Select "Xóa" from SelectedAction | Delete mode activated                               |
| 3    | Select "Ca Sáng"                 | Shift selected                                      |
| 4    | Click Delete button              | CanDelete() queries Booking table for FK references |

### Pass/Fail Criteria

| Criteria           | Pass               | Fail       |
| ------------------ | ------------------ | ---------- |
| Query executed     | ✓ Check performed  | ✗ No check |
| FK validated       | ✓ References found | ✗ Ignored  |
| Appropriate action | ✓ Proceed/Block    | ✗ Error    |
