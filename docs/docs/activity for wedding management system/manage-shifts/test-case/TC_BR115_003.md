# Test Case TC_BR115_003

## Verify CanDelete() returns false if shift is referenced

| Test Case ID | Test Case Description                                   | PreRequisites                                                                                                | Steps | Steps to Perform                                                                                                              | Step Expected Result                                                                                                                       | Test Case Expected Result                       | Actual Result | Status | Note |
| ------------ | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ----- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------- | ------------- | ------ | ---- |
| TC_BR115_003 | Verify CanDelete() returns false if shift is referenced | 1. WMS application is installed<br>2. User is logged in (Staff/Admin)<br>3. Shift used in at least 1 booking | 4     | 1. Navigate to ShiftView<br>2. Select "Xóa" from SelectedAction<br>3. Select referenced shift<br>4. Verify CanDelete() result | 1. ShiftView displayed<br>2. Delete mode activated<br>3. Referenced shift selected<br>4. CanDelete() returns false, delete button disabled | CanDelete() returns false for referenced shifts |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                      |
| ------------------ | -------------------------- |
| **Test Case ID**   | TC_BR115_003               |
| **Requirement ID** | BR115                      |
| **Use Case**       | UC 2.1.3.24 - Delete Shift |
| **Test Type**      | Functional / Logic         |
| **Priority**       | High                       |
| **Automation**     | Manual                     |

### CanDelete Implementation

```csharp
private bool CanDelete(int shiftId)
{
    var bookingCount = _context.Bookings.Count(b => b.ShiftId == shiftId);
    return bookingCount == 0;
}
```

### Detailed Steps

| Step | Steps to Perform                 | Step Expected Result                              |
| ---- | -------------------------------- | ------------------------------------------------- |
| 1    | Navigate to ShiftView            | ShiftView displayed                               |
| 2    | Select "Xóa" from SelectedAction | Delete mode activated                             |
| 3    | Select referenced shift          | Referenced shift selected                         |
| 4    | Verify CanDelete() result        | CanDelete() returns false, delete button disabled |

### Pass/Fail Criteria

| Criteria       | Pass              | Fail           |
| -------------- | ----------------- | -------------- |
| Returns false  | ✓ Correct         | ✗ Returns true |
| Delete blocked | ✓ Button disabled | ✗ Enabled      |
| Data protected | ✓ FK intact       | ✗ Orphaned     |
