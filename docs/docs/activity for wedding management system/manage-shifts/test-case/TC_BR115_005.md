# Test Case TC_BR115_005

## Verify CanDelete() returns true when no references exist

| Test Case ID | Test Case Description                                    | PreRequisites                                                                                             | Steps | Steps to Perform                                                                                                                     | Step Expected Result                                                                                                                   | Test Case Expected Result                        | Actual Result | Status | Note |
| ------------ | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------ | ------------- | ------ | ---- |
| TC_BR115_005 | Verify CanDelete() returns true when no references exist | 1. WMS application is installed<br>2. User is logged in (Staff/Admin)<br>3. Shift "Ca Tối" has 0 bookings | 4     | 1. Navigate to ShiftView<br>2. Select "Xóa" from SelectedAction<br>3. Select "Ca Tối" (unused shift)<br>4. Verify CanDelete() result | 1. ShiftView displayed<br>2. Delete mode activated<br>3. Unused shift selected<br>4. CanDelete() returns true, proceed to confirmation | CanDelete() returns true for unreferenced shifts |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                      |
| ------------------ | -------------------------- |
| **Test Case ID**   | TC_BR115_005               |
| **Requirement ID** | BR115                      |
| **Use Case**       | UC 2.1.3.24 - Delete Shift |
| **Test Type**      | Functional / Positive      |
| **Priority**       | High                       |
| **Automation**     | Manual                     |

### CanDelete Logic

```csharp
private bool CanDelete(int shiftId)
{
    return !_context.Bookings.Any(b => b.ShiftId == shiftId);
}

// Returns true when:
// - No bookings reference this shift
// - Shift is safe to delete
```

### Detailed Steps

| Step | Steps to Perform                 | Step Expected Result                              |
| ---- | -------------------------------- | ------------------------------------------------- |
| 1    | Navigate to ShiftView            | ShiftView displayed                               |
| 2    | Select "Xóa" from SelectedAction | Delete mode activated                             |
| 3    | Select "Ca Tối" (unused shift)   | Unused shift selected                             |
| 4    | Verify CanDelete() result        | CanDelete() returns true, proceed to confirmation |

### Pass/Fail Criteria

| Criteria          | Pass         | Fail            |
| ----------------- | ------------ | --------------- |
| Returns true      | ✓ Correct    | ✗ Returns false |
| Proceed allowed   | ✓ Continue   | ✗ Blocked       |
| Show confirmation | ✓ MSG81 next | ✗ Skipped       |
