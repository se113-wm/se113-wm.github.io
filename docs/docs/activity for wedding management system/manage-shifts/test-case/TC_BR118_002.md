# Test Case TC_BR118_002

## Verify other state flags set to FALSE

| Test Case ID | Test Case Description                 | PreRequisites                                                                                      | Steps | Steps to Perform                                                                                                | Step Expected Result                                                                                                     | Test Case Expected Result                       | Actual Result | Status | Note |
| ------------ | ------------------------------------- | -------------------------------------------------------------------------------------------------- | ----- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------- | ------------- | ------ | ---- |
| TC_BR118_002 | Verify other state flags set to FALSE | 1. WMS application is installed<br>2. User is logged in (Staff/Admin)<br>3. Previously in Add mode | 3     | 1. Navigate to ShiftView in Add mode<br>2. Select "Xuất Excel" from SelectedAction<br>3. Verify all state flags | 1. IsAdding = TRUE initially<br>2. "Xuất Excel" selected<br>3. IsExporting = TRUE, IsAdding/IsEditing/IsDeleting = FALSE | Other state flags are FALSE when in export mode |               |        |      |

---

## Test Case Details

### Basic Information

| Attribute          | Value                                |
| ------------------ | ------------------------------------ |
| **Test Case ID**   | TC_BR118_002                         |
| **Requirement ID** | BR118                                |
| **Use Case**       | UC 2.1.3.25 - Export Shifts to Excel |
| **Test Type**      | Functional / State                   |
| **Priority**       | Medium                               |
| **Automation**     | Manual                               |

### State Management

```csharp
private void OnActionChanged()
{
    IsAdding = false;
    IsEditing = false;
    IsDeleting = false;
    IsExporting = false;

    switch (SelectedAction)
    {
        case "Xuất Excel":
            IsExporting = true;
            break;
        // ... other cases
    }
}
```

### Detailed Steps

| Step | Steps to Perform                        | Step Expected Result                                      |
| ---- | --------------------------------------- | --------------------------------------------------------- |
| 1    | Navigate to ShiftView in Add mode       | IsAdding = TRUE initially                                 |
| 2    | Select "Xuất Excel" from SelectedAction | "Xuất Excel" selected                                     |
| 3    | Verify all state flags                  | IsExporting = TRUE, IsAdding/IsEditing/IsDeleting = FALSE |

### Pass/Fail Criteria

| Criteria           | Pass    | Fail         |
| ------------------ | ------- | ------------ |
| IsAdding = FALSE   | ✓ Reset | ✗ Still true |
| IsEditing = FALSE  | ✓ Reset | ✗ Still true |
| IsDeleting = FALSE | ✓ Reset | ✗ Still true |
